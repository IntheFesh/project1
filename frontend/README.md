# Frontend

**CORE (Phase 2):** a Gradio / Streamlit chat UI that shows the live tool calls and the final
answer for the agent. State is kept in app/server memory only — **no** browser
`localStorage` / `sessionStorage`.

**STRETCH (Phase 8):** a Next.js frontend.

Added in Phase 2 (`frontend/app.py`).
