# Training deps — install ONLY on the Blackwell GPU box (sm_120). NOT in the uv lock.
#
# Torch MUST come from the CUDA 13.0 (cu130) index. cu124/cu126/cu128 wheels do not
# provide working sm_120 kernels here and fail at runtime: "no kernel image is available
# for execution on the device". The verified working stack is in BLACKWELL_NOTES.md.
#
#   1) torch from the cu130 index:
#        uv pip install torch torchvision --index-url https://download.pytorch.org/whl/cu130
#   2) verify sm_120 is present:
#        python -c "import torch; print(torch.cuda.get_arch_list())"   # must contain sm_120
#   3) then install the rest:
#        uv pip install -r requirements/train.txt

transformers>=5.10           # the run used 5.10
peft>=0.13
trl>=1.5                     # 1.5.x renamed SFTConfig.max_seq_length -> max_length
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.49.2         # QLoRA 4-bit base on sm_120 (RTX 5090)
